<p id="notice"><%= notice %></p>
<nav class="navbar bg-light">
  <div class="container-fluid">
    <%= link_to 'Simple AirBnB', flats_path %>
    <form class="d-flex" role="search">
      <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success" type="submit">Search</button>
    </form>
  </div>
</nav>
<h1>Flats</h1>

<table>

  <div class="card" style="width: 18rem;">
    <img src='https://images.unsplash.com/photo-1502672260266-1c1ef2d93688?w=1200' class="card-img-top" alt="...">
  <div class="card-body">
    <% @flats.each do |flat| %>
      <h5 class="card-title"><%= flat.name %></h5>
      <ul class="list-group list-group-flush">
      <li class="list-group-item"><%= flat.address %></li>
      <li class="list-group-item"><%= flat.description %></li>
      <li class="list-group-item"><%= flat.price_per_night %><%= flat.number_of_guests %></li>
    <% end %>
  </ul>
       <div class="card-body">
        <a href="#" class="card-link">Card link</a>
        <a href="#" class="card-link">Another link</a>
    </div>
  </div>
</div>
  <tbody>


    <h5 class="card-title">Card title</h5>
    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
  </div>

  <div class="card-body">
    <a href="#" class="card-link">Card link</a>
    <a href="#" class="card-link">Another link</a>
  </div>
</div>


      <% @flats.each do |flat| %>
        <tr>
          <td><%= flat.name %></td>
          <td><%= flat.address %></td>
          <td><%= flat.description %></td>
          <td><%= flat.price_per_night %></td>
          <td><%= flat.number_of_guests %></td>
          <td><%= link_to 'Show', flat %></td>
          <td><%= link_to 'Edit', edit_flat_path(flat) %></td>
          <td><%= link_to 'Destroy', flat, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Flat', new_flat_path %>
